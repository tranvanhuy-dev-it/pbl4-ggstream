# Coturn Setup (Local, No Docker)

**Status: planned, Milestone 5.** This guide will cover installing Coturn
directly on the host (Windows or a separate Linux box), configuring the
shared secret, opening the required UDP/TCP ports, and testing relay
candidates end-to-end — once STUN-only WebRTC (Milestone 4) is working and
stable.
